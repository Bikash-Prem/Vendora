services:
  - type: web
    name: vendora-backend
    env: python
    rootDir: BACKEND
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
